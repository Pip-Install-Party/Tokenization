// tokenize.h
#ifndef TOKENIZE_H // include guard
#define TOKENIZE_H 

#include <iostream>
#include <fstream>
#include <string>
#include <filesystem>
#include <sstream> 

class Tokenize
{
private:

public:
    Tokenize() {}
    ~Tokenize() { delete this; };
};

#endif /* TOKENIZE_H */
